# Compiled rule catalog

> Generated by `adapters/claude-code/build.mjs` from the neutral guidance source. Do not edit by hand.

2 active rules · 2 block (required + ci-gate/managed-platform).

Classification legend: **BLOCK** = must fix before merge · **WARN** = fix recommended ·
**REQUIRED (unenforceable)** = mandated but no gate, held by review/culture · **ADVISORY** = recommended.

## Architecture & Tech Stack

**arch-no-nodejs** — Node.js and JavaScript runtimes are prohibited  
`Strategic` · `ci-gate` · agent: `enforce` · BLOCK  
Rule: Services **must not** be implemented on the Node.js / JavaScript runtime. Any of the following is a violation: a `package.json`, a `node_modules/` directory, a Node lockfile (`package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`), `.js` / `.mjs` / `.cjs` / `.ts` source files, or an `import`/`require` of a `node:` builtin.

**arch-python-required** — Services must be implemented in Python  
`Strategic` · `ci-gate` · agent: `enforce` · BLOCK  
Rule: Every service **must** be implemented in **Python**. A conforming service has Python source (`.py`) and a Python project descriptor (`pyproject.toml` or `requirements.txt`), and no other-language runtime. A service that is not Python — no `.py` entrypoint — violates this rule.
